export interface MenuOption {
    element_id: string;
    title: string;
};

export interface Project {
    element_id: string;
    header: string;
    paragraph: string;
    code: string;
    github: string;
    siteURL: string;
    images: string[];
};
