export interface HeroOption {
    element_id: string;
    title: string;
};

export interface Project {
    divID: string;
    header: string;
    paragraph: string;
    code: string;
    github: string;
    siteURL: string;
    images: string[][];
};
