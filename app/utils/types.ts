export interface IconRow {
    id: number;
    header: string;
    images: string[][];
};

export interface Project {
    id: number;
    header: string;
    paragraph: string;
    images: string[][];
    github: string;
    siteURL: string;
    element_id: string;
    image: string;
};