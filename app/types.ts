export interface Experience {
    header: string;
    images: string[][];
};

export interface Project {
    id: number;
    image: string;
    header: string;
    paragraph: string;
    images: string[][];
    github: string;
    siteURL: string;
};